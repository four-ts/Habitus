

export function CreateMilestone(title, isCompleted, startDate, endDate, tasks) {
    this.title = title,
        this.isCompleted = isCompleted,
        this.startDate = startDate,
        this.endDate = endDate,
        this.tasks = tasks
}